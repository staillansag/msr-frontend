apiVersion: v1
kind: Secret
metadata:
  name: environment-secret
type: Opaque
data:
  WM_DB_PASSWORD: mdp base WM encodé en base 64
  DB_PASSWORD: mdp base microservice encodé en base 64
  SFTP_USER_PASSWORD: mdp sftp encodé en base 64
  S3_ACCESS_KEY: access key s3 encodée en base 64
  S3_SECRET_KEY: access secret s3 encodée en base 64
  ADMIN_PASSWORD: mdp du user Administrator encode en base 64
